// I used as help the code described by the mentor in the link below:

// https://knowledge.udacity.com/questions/253741#253791

import { visiblePage } from "../src/client/js/visiblePage"

describe("Testing the submit functionality for user input", () => {
    test("Testing the visiblePage() function", () => {
        expect(visiblePage).toBeDefined();
})});